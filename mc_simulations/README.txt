To replicate all tables with the simulation results, keep "n_rep <- 1000" in each R-file (this sets the number of replications to 1000). 

Note that for the tables in the directory "output_tables", for the sake of computational time, n_rep = 10. 